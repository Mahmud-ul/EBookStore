@model IEnumerable<EBookStore.Models.Payment>

@{
    ViewData["Title"] = "Payments";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="container">
    <!-- Page title and Create button -->
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-start mb-3">
        <h3 class="fw-bold mb-2 mb-md-0"><i class="bi bi-credit-card-fill me-2"></i> Payments</h3>
        <a asp-action="Create" class="btn btn-success btn-sm">
            <i class="bi bi-plus-lg me-1"></i> Create New
        </a>
    </div>

<<<<<<< HEAD
<p>
    <a asp-action="Create">Create New</a>
</p>
<table id="example" class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PaymentDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Order)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DelivaryCharge)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TotalAmount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Extra)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PaymentDate)
            </td>
            <td>
                @(item.Order != null ? item.Order.ID : "")
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DelivaryCharge)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TotalAmount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Extra)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
=======
    <div class="card shadow-lg border-0 rounded-4">
        <div class="card-header bg-gradient-secondary rounded-top">
            <small>All Payments (Total: @(Model?.Count() ?? 0))</small>
        </div>

        <div class="card-body">
            @if (Model != null && Model.Any())
            {
                <div class="table-responsive">
                    <table id="example" class="table table-hover table-striped align-middle">
                        <thead class="table-light">
                            <tr>
                                <th>Payment Date</th>
                                <th>Order ID</th>
                                <th>Delivery Charge</th>
                                <th>Total Amount</th>
                                <th>Extra</th>
                                <th class="text-center">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.PaymentDate.ToString("yyyy-MM-dd")</td>
                                    <td>#@(10000 + item.OrderID)</td>
                                    <td>@item.DelivaryCharge</td>
                                    <td>@item.TotalAmount</td>
                                    <td>@item.Extra</td>
                                    <td class="text-center">
                                        <div class="btn-group" role="group">
                                            <a asp-action="Details"
                                               asp-route-id="@item.ID"
                                               class="btn btn-sm btn-info text-white"
                                               title="View Details">
                                                <i class="bi bi-eye-fill"></i>
                                            </a>
                                            <a asp-action="Edit"
                                               asp-route-id="@item.ID"
                                               class="btn btn-sm btn-warning text-white"
                                               title="Edit">
                                                <i class="bi bi-pencil-fill"></i>
                                            </a>
                                            <a asp-action="Delete"
                                               asp-route-id="@item.ID"
                                               class="btn btn-sm btn-danger"
                                               title="Delete"
                                               onclick="return confirm('Are you sure you want to delete this payment?');">
                                                <i class="bi bi-trash-fill"></i>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="text-center py-5">
                    <i class="bi bi-credit-card text-muted display-4 mb-3"></i>
                    <h5 class="text-muted">No payments found</h5>
                    <p class="mb-3">Start by creating your first payment record</p>
                    <a asp-action="Create" class="btn btn-success btn-sm">
                        <i class="bi bi-plus-lg me-2"></i> Create New Payment
                    </a>
                </div>
            }
        </div>
    </div>
</div>

<style>
    .card {
        border: none;
        border-radius: 8px;
    }

    .card-header {
        border-bottom: 1px solid #e9ecef;
    }

    .table th {
        font-weight: 600;
        text-transform: uppercase;
        font-size: 0.85rem;
        letter-spacing: 0.5px;
    }

    .table-hover tbody tr:hover {
        background-color: #f8f9fa;
        transition: background-color 0.2s ease;
    }

    .btn-group .btn {
        padding: 0.35rem 0.7rem;
    }
</style>

@section Scripts {
    <script>
        $(document).ready(function () {
            $('#example').DataTable({
                "order": [[0, "desc"]],
                "pageLength": 25,
                "language": {
                    "search": "Search Payments:",
                    "lengthMenu": "Show _MENU_ payments per page",
                    "info": "Showing _START_ to _END_ of _TOTAL_ payments",
                    "infoEmpty": "No payments available",
                    "infoFiltered": "(filtered from _MAX_ total payments)"
                },
                "columnDefs": [
                    { "orderable": false, "targets": [5] }
                ]
            });
        });
    </script>
>>>>>>> 199720b95032cbd73f24c22ad0fcacea95219641
}
