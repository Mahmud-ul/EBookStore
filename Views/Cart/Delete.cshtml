@model EBookStore.Models.Cart

@{
    ViewData["Title"] = "Delete Cart Item";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="container my-5">
    <div class="card shadow-lg border-0 rounded-4">
        <!-- Card header -->
        <div class="card-header text-white rounded-top-4" style="background: linear-gradient(90deg, #dc3545, #b02a37);">
            <h4 class="mb-0"><i class="bi bi-exclamation-triangle-fill me-2"></i> Delete Cart Item</h4>
        </div>

        <!-- Card body -->
        <div class="card-body p-4">
            <h5 class="mb-4">Are you sure you want to delete this cart item?</h5>

            <dl class="row mb-4">
                <dt class="col-sm-3 fw-semibold">@Html.DisplayNameFor(model => model.ID)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.ID)</dd>

                <dt class="col-sm-3 fw-semibold">@Html.DisplayNameFor(model => model.Product)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.Product.Name)</dd>

                <dt class="col-sm-3 fw-semibold">@Html.DisplayNameFor(model => model.User)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.User.Email)</dd>

                <dt class="col-sm-3 fw-semibold">@Html.DisplayNameFor(model => model.Quantity)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.Quantity)</dd>
            </dl>

            <div class="d-flex gap-3">
                <form asp-action="Delete">
                    <input type="hidden" asp-for="ProductID" />
                    <input type="hidden" asp-for="UserID" />
                    <button type="submit" class="btn btn-danger rounded-pill px-4 shadow-sm">
                        <i class="bi bi-trash-fill me-1"></i> Delete
                    </button>
                </form>
                <a asp-action="Index" class="btn btn-outline-secondary rounded-pill px-4">
                    <i class="bi bi-arrow-left-circle me-1"></i> Back
                </a>
            </div>
        </div>
    </div>
</div>
